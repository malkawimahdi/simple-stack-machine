begin
   main(1, 0, 0);
end

fun main(one, x, y) {
  if (x == 0) then
    a = 0;
  else
    a = 1;
  endif
  if (y == 0) then
    b = 0;
  else
    b = 1;
  endif
  if (a * (b == 0)) then
    printint one;
  else
    if (b * (a == 0)) then
      printint one;
    else
      printint 1 - one;
    endif
  endif
}
